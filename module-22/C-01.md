## 🟦 Prisma: আধুনিক Node.js ও TypeScript ORM এর পূর্ণাঙ্গ পরিচয়

### 1️⃣ Prisma কী? (What is Prisma?)

Prisma হলো একটি আধুনিক এবং টাইপ-সেফ **ORM (Object Relational Mapping)** টুল। এটি মূলত আপনার অ্যাপ্লিকেশন কোড (JavaScript/TypeScript) এবং ডাটাবেসের মধ্যে একটি সেতু হিসেবে কাজ করে।

Prisma ব্যবহার করলে আপনাকে কাঁচা (Raw) SQL কুয়েরি নিয়ে চিন্তা করতে হয় না। এর পরিবর্তে আপনি নিচের মতো ক্লিন কোড দিয়ে ডাটাবেস হ্যান্ডেল করতে পারেন:

- **Create:** `prisma.user.create()`
- **Read:** `prisma.user.findMany()`
- **Update:** `prisma.user.update()`
- **Delete:** `prisma.user.delete()`

---

### 2️⃣ Prisma কেন গুরুত্বপূর্ণ? (Importance of Prisma)

🔹 **১. Type-Safety:** Prisma-এর সবচেয়ে বড় শক্তি হলো এটি TypeScript-এর সাথে চমৎকারভাবে কাজ করে। ভুল কুয়েরি লিখলে কোড রান করার আগেই IDE আপনাকে এরর দেখাবে।
🔹 **২. Auto-Completion (IntelliSense):** আপনি যখন `prisma.` লিখবেন, তখন আপনার ডাটাবেস মডেল অনুযায়ী এটি সব ফিল্ডের নাম সাজেস্ট করবে।
🔹 **৩. Prisma Migrate:** ডাটাবেসের স্ট্রাকচার পরিবর্তন করা (যেমন নতুন কলাম যোগ করা) এবং তার ইতিহাস (Version Control) রাখা খুব সহজ।
🔹 **৪. Readable Queries:** SQL-এর বিশাল বিশাল জয়েন (Join) কুয়েরি না লিখে সহজেই রিলেশনাল ডাটা আনা যায়।
🔹 **৫. Multi-Database Support:** আপনি চাইলে সহজেই PostgreSQL, MySQL, SQLite, বা MongoDB-তে সুইচ করতে পারেন।

---

### 3️⃣ Prisma-এর প্রধান ৩টি স্তম্ভ (Core Components)

Prisma মূলত তিনটি প্রধান অংশ নিয়ে গঠিত:

1. **Prisma Schema (`schema.prisma`):** এখানে আপনি আপনার ডাটাবেস কানেকশন এবং ডেটা মডেল (Table Structure) ডিফাইন করেন।
2. **Prisma Migrate:** এটি আপনার মডেল অনুযায়ী ডাটাবেসে টেবিল তৈরি বা আপডেট করে।
3. **Prisma Client:** এটি একটি অটো-জেনারেটেড কুয়েরি বিল্ডার, যা দিয়ে আপনি আপনার অ্যাপ থেকে ডাটা রিড/রাইট করেন।

---

### 4️⃣ একটি বাস্তব উদাহরণ

**ধাপ ১: মডেল তৈরি (`schema.prisma`)**

```prisma
model User {
  id    Int     @id @default(autoincrement())
  name  String
  email String  @unique
  posts Post[]  // Relation: একজন ইউজারের অনেক পোস্ট থাকতে পারে
}

model Post {
  id        Int     @id @default(autoincrement())
  title     String
  authorId  Int
  author    User    @relation(fields: [authorId], references: [id])
}

```

**ধাপ ২: কুয়েরি চালানো (Prisma Client)**

```ts
const newUser = await prisma.user.create({
  data: {
    name: "Gemini",
    email: "gemini@example.com",
  },
});
```

---

### 🟩 সংক্ষেপে (Summary)

Prisma হলো ডেভেলপারদের জন্য একটি আশীর্বাদ কারণ এটি:

- SQL শেখার চাপ কমায়।
- ডেভেলপমেন্টের গতি কয়েক গুণ বাড়িয়ে দেয়।
- টাইপ-সেফটির মাধ্যমে বড় ধরণের বাগ (Bug) হওয়া রোধ করে।

Node.js এবং বিশেষ করে TypeScript ইকোসিস্টেমে বর্তমানে Prisma সবচেয়ে জনপ্রিয় ও আধুনিক ORM।
