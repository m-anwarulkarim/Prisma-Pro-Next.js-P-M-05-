## 🔍 Prisma: Reading Data (SELECT , INCLUDE) — Full Guide

Prisma-তে ডাটাবেস থেকে তথ্য সংগ্রহের সময় রেসপন্স ডেটাকে নিয়ন্ত্রণ করার জন্য `select` এবং `include` দুটি অপরিহার্য টুল। নিচে এদের বিস্তারিত ব্যবহার ও পার্থক্য আলোচনা করা হলো।

---

### 1️⃣ SELECT: নির্দিষ্ট কলাম নির্বাচন করা

যখন আপনার পুরো টেবিলের সব ডাটা প্রয়োজন নেই, বরং শুধুমাত্র নির্দিষ্ট কয়েকটি কলাম দরকার, তখন `select` ব্যবহার করা হয়।

**কোড উদাহরণ:**

```typescript
const users = await prisma.user.findMany({
  select: {
    id: true,
    name: true,
    // password: false (ডিফল্টভাবে সবকিছুই false থাকে, যা true করবেন শুধু সেটাই আসবে)
  },
});
```

**সুবিধাসমূহ:**

- **Performance:** ডাটাবেস থেকে কম ডাটা ফেচ হয়, ফলে কুয়েরি দ্রুত কাজ করে।
- **Security:** পাসওয়ার্ড বা সেনসিটিভ ডাটা ভুলে এপিআই রেসপন্সে চলে যাওয়া রোধ করে।
- **Memory Usage:** ব্রাউজার বা ক্লায়েন্ট সাইডে মেমোরি কম খরচ হয়।

---

### 2️⃣ INCLUDE: রিলেশনাল ডাটা ফেচ করা

যখন একটি টেবিলের সাথে সম্পর্কিত অন্য টেবিলের ডাটা (যেমন: User-এর সাথে তার সব Post) একবারে আনতে হয়, তখন `include` ব্যবহার করা হয়।

**কোড উদাহরণ:**

```typescript
const userWithPosts = await prisma.user.findUnique({
  where: { id: 1 },
  include: {
    posts: true, // ইউজারের সাথে তার সব পোস্টগুলোও চলে আসবে
  },
});
```

**Nested Include:** আপনি চাইলে রিলেশনের ভেতরেও রিলেশন আনতে পারেন।

```typescript
const data = await prisma.user.findUnique({
  where: { id: 1 },
  include: {
    posts: {
      include: {
        comments: true, // পোস্টের ভেতরে আবার কমেন্টগুলোও আসবে
      },
    },
  },
});
```

---

### 3️⃣ SELECT + INCLUDE কি একসাথে ব্যবহার করা যায়?

**না।** Prisma-তে একই লেভেলে `select` এবং `include` একসাথে ব্যবহার করা সম্ভব নয়। তবে রিলেশনাল ডাটার নির্দিষ্ট কলাম আনতে চাইলে আপনি **Nested Select** ব্যবহার করতে পারেন। এটিই ইন্ডাস্ট্রি স্ট্যান্ডার্ড।

**Nested Select উদাহরণ:**

```typescript
const data = await prisma.user.findUnique({
  where: { id: 1 },
  select: {
    id: true,
    name: true,
    posts: {
      select: {
        title: true, // পোস্টের সব ডাটা না এনে শুধু টাইটেল আনা হচ্ছে
      },
    },
  },
});
```

---

### 📊 তুলনামূলক পার্থক্য (At a Glance)

| বৈশিষ্ট্য         | SELECT                                         | INCLUDE                          |
| ----------------- | ---------------------------------------------- | -------------------------------- |
| **মূল কাজ**       | নির্দিষ্ট কলাম/ফিল্ড নির্বাচন করা              | সম্পর্কিত টেবিলের ডাটা যুক্ত করা |
| **রেসপন্স সাইজ**  | সাধারণত ছোট এবং অপ্টিমাইজড                     | রিলেশনের কারণে বড় হতে পারে       |
| **ব্যবহারের সময়** | যখন শুধু নির্দিষ্ট তথ্য দরকার                  | যখন রিলেটেড অবজেক্টসহ তথ্য দরকার |
| **টাইপ সেফটি**    | রিটার্ন টাইপ শুধু সিলেক্ট করা ফিল্ডগুলোই দেখায় | রিটার্ন টাইপে রিলেটেড মডেলও থাকে |

---

### ✅ প্রফেশনাল টিপস:

১. **পাসওয়ার্ড প্রোটেকশন:** সবসময় `User` মডেল থেকে ডাটা নেওয়ার সময় `select` ব্যবহার করে পাসওয়ার্ড ফিল্ডটি বাদ রাখুন।
২. **Performance Optimization:** রিলেশনাল ডাটা অনেক বেশি হলে (যেমন ১০০০+ পোস্ট) `include` ব্যবহার না করে কুয়েরিকে পেজিনেট (Pagination) করার চেষ্টা করুন।
৩. **Clean Code:** কোডের রিডাবিলিটি বাড়ানোর জন্য বড় নেস্টেড কুয়েরিগুলোকে আলাদা ভেরিয়েবলে রেখে তারপর `find` মেথডে পাস করুন।
