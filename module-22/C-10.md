## 🗑️ Prisma: Table, Column, and Database Deletion Guide

Prisma-তে কোনো কিছু ডিলিট করার মূল মন্ত্র হলো: **আগে স্কিমা (Schema) পরিবর্তন করা, তারপর মাইগ্রেশন (Migration) চালানো।**

---

### 🟦 ১) টেবিল (Table) ডিলিট করার নিয়ম

যদি আপনার ডাটাবেস থেকে কোনো নির্দিষ্ট টেবিল (যেমন: `User`) পুরোপুরি মুছে ফেলতে চান:

1. **স্কিমা পরিবর্তন:** `schema.prisma` ফাইল থেকে পুরো `model User { ... }` ব্লকটি মুছে দিন।
2. **মাইগ্রেশন চালানো:** টার্মিনালে নিচের কমান্ডটি দিন:

```bash
npx prisma migrate dev --name remove_user_table

```

**পেছনের লজিক:**
Prisma ডাটাবেসে স্বয়ংক্রিয়ভাবে `DROP TABLE "User";` SQL কুয়েরি চালাবে, যা টেবিলটি চিরতরে মুছে দেবে।

---

### 🟩 ২) কলাম (Column) ডিলিট করার নিয়ম

ধরা যাক `User` টেবিলে `phone` নামে একটি কলাম আছে যা এখন আর প্রয়োজন নেই:

1. **স্কিমা পরিবর্তন:** `schema.prisma` ফাইলে `User` মডেলের ভেতর থেকে `phone String?` লাইনটি মুছে ফেলুন।
2. **মাইগ্রেশন চালানো:** টার্মিনালে নিচের কমান্ডটি দিন:

```bash
npx prisma migrate dev --name delete_phone_column

```

**পেছনের লজিক:**
Prisma ডাটাবেসে `ALTER TABLE "User" DROP COLUMN "phone";` কুয়েরি চালাবে। এর ফলে টেবিলটি থাকবে কিন্তু শুধু নির্দিষ্ট কলামটি মুছে যাবে।

---

### 🟥 ৩) পুরো ডাটাবেস (Database) ডিলিট বা রিসেট

যদি আপনি ডাটাবেসের সব টেবিল এবং সব ডেটা মুছে একদম নতুন করে শুরু করতে চান, তবে আপনাকে ডাটাবেস **Reset** করতে হবে।

**কমান্ড:**

```bash
npx prisma migrate reset

```

**এই কমান্ডটি যা যা করবে:**

- ✔ পুরো ডাটাবেসটি ড্রপ (Drop) করবে।
- ✔ আবার নতুন করে ডাটাবেস তৈরি করবে।
- ✔ আপনার সব মাইগ্রেশন ফাইলগুলো শুরু থেকে অ্যাপ্লাই করবে।
- ✔ **সতর্কতা:** ডাটাবেসে থাকা আগের সব ডেটা চিরতরে মুছে যাবে।

---

### ⚠ গুরুত্বপূর্ণ টিপস (Best Practices)

1. **ডেটা ব্যাকআপ:** টেবিল বা কলাম ডিলিট করার আগে প্রয়োজনীয় ডেটা ব্যাকআপ নিয়ে নিন, কারণ মাইগ্রেশন রান করার পর ডেটা ফিরে পাওয়া সম্ভব নয়।
2. **Production Warning:** কখনোই প্রোডাকশন সার্ভারে `migrate reset` চালাবেন না। প্রোডাকশনে শুধুমাত্র `npx prisma migrate deploy` ব্যবহার করা উচিত।
3. **শর্টকাট:** যদি শুধু টেস্ট ডেটা মুছতে চান কিন্তু টেবিল বা স্ট্রাকচার ঠিক রাখতে চান, তবে Prisma Studio ওপেন করে ম্যানুয়ালি ডেটা ডিলিট করতে পারেন।
