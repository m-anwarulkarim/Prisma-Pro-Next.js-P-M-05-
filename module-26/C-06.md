## 🚩 Filtering Data Using Boolean Fields

Boolean ফিল্টার মূলত `true` অথবা `false` এই দুটি মানের উপর ভিত্তি করে কাজ করে। Prisma-তে আমরা `where` ক্লজ-এর ভেতর সরাসরি এই ফিল্ডগুলো ব্যবহার করতে পারি।

### 🛠️ কোড ইমপ্লিমেন্টেশন

ধরা যাক, আমাদের একটি ব্লগ অ্যাপ আছে যেখানে আমরা শুধুমাত্র সেই পোস্টগুলো দেখাতে চাই যেগুলো **পাবলিশড (Published)** করা হয়েছে।

```ts
import { PrismaClient } from "@prisma/client";

const prisma = new PrismaClient();

async function getPublishedPosts() {
  const posts = await prisma.post.findMany({
    where: {
      // Boolean Field Filter (Static)
      isPublished: true, // শুধুমাত্র true হওয়া ডাটাগুলো আসবে
    },
  });

  return posts;
}
```

---

### 🧠 মূল কনসেপ্টসমূহ (Key Concepts)

#### ১. সরাসরি মান ব্যবহার (Direct Matching)

Boolean ফিল্ডে `contains` বা `mode` এর মতো জটিল অপারেটর প্রয়োজন হয় না। এখানে সরাসরি `true` অথবা `false` লিখে দিলেই Prisma সেই অনুযায়ী ডাটা ফিল্টার করে।

#### ২. ডাইনামিক ফিল্টারিং (Dynamic Boolean Filter)

URL (req.query) থেকে আসা মানের উপর ভিত্তি করে ফিল্টার করার সময় কিছু বিষয় মাথায় রাখতে হয়:

```ts
const { showArchived } = req.query; // এটি "true" বা "false" (String) হিসেবে আসে

const posts = await prisma.post.findMany({
  where: {
    /**
     * ⚠️ সমস্যা সমাধান:
     * যদি ইউজার কিছু না পাঠায় (undefined), তবে সব পোস্ট দেখাবে।
     * যদি পাঠায়, তবে সেটাকে Boolean এ রূপান্তর করবে।
     */
    ...(showArchived !== undefined && {
      isArchived: showArchived === "true",
    }),
  },
});
```

**মনে রাখুন:**

- যদি কোড-এ সরাসরি **True/False** লিখে দেন কোনো রূপান্তর লাগবে না।
- যদি **URL (req.query)** বা **Form (req.body)** থেকে মান আসে সেটি String থাকে, তাই অবশ্যই **Boolean-এ** রূপান্তর করতে হবে (`isPublished === "true"` এর মাধ্যমে)।
- **Undefined Handling:** যদি ফিল্টারটি ঐচ্ছিক (Optional) হয়, তবে Spread Operator `...` ব্যবহার করা ভালো যাতে প্যারামিটার না পাঠালে সব ডাটা পাওয়া যায়।

---

### 📊 বাস্তব জীবনের উদাহরণ (Use Cases)

| ফিল্ডের নাম   | ফিল্টার করার উদ্দেশ্য                       | কোড উদাহরণ (Dynamic)                            |
| ------------- | ------------------------------------------- | ----------------------------------------------- |
| `isPublished` | শুধুমাত্র লাইভ পোস্টগুলো দেখানোর জন্য।      | `isPublished: req.query.isPublished === "true"` |
| `isAdmin`     | শুধুমাত্র অ্যাডমিন ইউজারদের খুঁজে বের করতে। | `isAdmin: req.query.isAdmin === "true"`         |
| `isDeleted`   | ডিলিট হওয়া পোস্টগুলো বাদ দিতে।             | `isDeleted: false`                              |
| `hasDiscount` | যে সব পণ্যে ছাড় আছে সেগুলো দেখাতে।         | `hasDiscount: req.query.discount === "true"`    |

---

### 💡 প্রো-টিপ: Boolean এর সাথে AND/OR কম্বিনেশন

সাধারণত Boolean ফিল্টার একা ব্যবহার না করে অন্য সার্চের সাথে মিশিয়ে ব্যবহার করা হয়।

**উদাহরণ:** "টাইটেল-এ 'JavaScript' আছে এমন সব **পাবলিশড** পোস্ট দেখাও।"

```ts
const posts = await prisma.post.findMany({
  where: {
    AND: [
      { isPublished: true }, // Boolean ফিল্টার
      {
        title: {
          contains: "JavaScript",
          mode: "insensitive",
        },
      },
    ],
  },
});
```

---

### ✅ কেন এটি গুরুত্বপূর্ণ?

১. **Security:** ড্রাফট মোডে থাকা বা আনপাবলিশড পোস্ট ভুল করে ইউজারের সামনে চলে যাওয়া ঠেকায়।
২. **Efficiency:** ডাটাবেস খুব দ্রুত Boolean মান প্রসেস করতে পারে, যা পারফরম্যান্স ভালো রাখে।
৩. **Simplicity:** কোড পড়তে খুব সহজ এবং লজিক ক্লিয়ার থাকে।
