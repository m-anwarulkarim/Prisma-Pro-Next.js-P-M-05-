## 🔐 Role-Based Auth Middleware (Better Auth + Express)

এই কোডটি মূলত **"Secure-by-Default"** নীতি অনুসরণ করে। অর্থাৎ:

- **আগে লগইন করো** (Session check)
- **ইমেইল ভেরিফাই করো** (Email check)
- **সঠিক রোল থাকতে হবে** (Role check)

এই তিনটি শর্ত পূরণ করলেই কেবল কোডটি `next()` ফাংশন কল করে ইউজারকে পরের ধাপে যাওয়ার অনুমতি দেয়।

```ts
import { NextFunction, Request, Response } from "express";
import { auth } from "../lib/auth";
import { fromNodeHeaders } from "better-auth/node";
import { ROLE } from "../types/role.type";

const authGurd = (...role: ROLE[]) => {
  return async (req: Request, res: Response, next: NextFunction) => {
    try {
      const session = await auth.api.getSession({
        headers: fromNodeHeaders(req.headers),
      });
      if (!session) {
        return res.status(401).json({
          success: false,
          message: "You are not authorized!",
        });
      }

      if (!session.user.emailVerified) {
        return res.status(403).json({
          success: false,
          message: "Email verification required. Please verfiy your email!",
        });
      }

      req.user = {
        id: session.user.id,
        email: session.user.email,
        name: session.user.name,
        role: session.user.role as string,
        emailVerified: session.user.emailVerified,
      };

      if (role.length && !role.includes(req.user.role as ROLE)) {
        return res.status(403).json({
          success: false,
          message:
            "Forbidden! You don't have permission to access this resources!",
        });
      }

      next();
    } catch (error: any) {
      res.status(400).json({
        success: false,
        message: error.message,
      });
    }
  };
};
export default authGurd;
```

নিচে এর প্রতিটি অংশ সহজভাবে ব্যাখ্যা করা হলো:

---

### ১. `fromNodeHeaders` এর ব্যবহার

```ts
const session = await auth.api.getSession({
  headers: fromNodeHeaders(req.headers),
});
```

Express-এর ডিফল্ট `req.headers` সরাসরি Better-Auth বুঝতে পারে না। তাই `fromNodeHeaders` ফাংশনটি ব্যবহার করে সেই হেডারগুলোকে Better-Auth-এর ফরম্যাটে রূপান্তর করা হচ্ছে, যাতে এটি ইউজারের সেশনটি (Login Session) সঠিকভাবে খুঁজে বের করতে পারে।

### ২. `...role: ROLE[]` (Rest Parameter)

ফাংশনটি এভাবে লেখা: `authGurd(...role: ROLE[])`
এর মানে হলো আপনি এই মিডলওয়্যারে কমা দিয়ে অনেকগুলো রোল পাঠাতে পারেন।
যেমন: `authGurd("admin", "manager")`। এর ফলে এটি একটি অ্যারো (Array) হিসেবে কোডের ভেতরে কাজ করবে।

### ৩. ধাপে ধাপে যাচাই (Validation Steps)

- **লগইন চেক:** যদি `session` না থাকে, তবে **401 Unauthorized** এরর দেবে।
- **ইমেইল ভেরিফিকেশন:** যদি ইউজারের ইমেইল ভেরিফাইড না থাকে (`!session.user.emailVerified`), তবে **403 Forbidden** এরর দেবে। এটি বাধ্যতামূলক করা হয়েছে।
- **ডাটা পাস:** সেশন থেকে ইউজারের প্রয়োজনীয় তথ্যগুলো (ID, Email, Role, Name) `req.user` অবজেক্টে সেট করে দেওয়া হচ্ছে, যাতে পরের ফাংশনগুলোতে সরাসরি ব্যবহার করা যায়।

### ৪. রোল বা পারমিশন চেক (Logic)

```ts
if (role.length && !role.includes(req.user.role as ROLE)) {
   return res.status(403).json({ ... message: "Forbidden!" });
}

```

এটিই হলো আসল ফিল্টার:

1. প্রথমে দেখে কোনো রোল চাওয়া হয়েছে কি না (`role.length`)।
2. এরপর চেক করে বর্তমান ইউজারের রোলটি আপনার দেওয়া ওই লিস্টে আছে কি না (`role.includes(...)`)।
3. যদি লিস্টে না থাকে, তবে তাকে **403 Forbidden** এরর দিয়ে আটকে দেয়।

---

### আগের কোডের সাথে এর মূল পার্থক্য কী?

১. **Better-Auth Integration:** এখানে `fromNodeHeaders` ব্যবহার করা হয়েছে যা Better-Auth-এর জন্য নির্দিষ্ট।
২. **লেভেল সিস্টেম নেই:** আগের কোডটিতে **ROLE_LEVEL** (1, 2, 3) ছিল। কিন্তু এই কোডটিতে লেভেল চেক নেই, সরাসরি রোল ম্যাচিং (`includes`) করা হচ্ছে।
৩. **ইমেইল ভেরিফিকেশন বাধ্যতামূলক:** এখানে ইমেইল ভেরিফিকেশন না থাকলে কোনোভাবেই ঢোকা যাবে না (অপশনাল রাখার সুযোগ নেই)।

### ব্যবহারের উদাহরণ:

```ts
// যে কেউ (লগইন করা ও ভেরিফাইড)
router.get("/profile", authGurd(), userController);

// শুধু এডমিন
router.post("/delete", authGurd("admin"), adminController);

// এডমিন অথবা ম্যানেজার যে কেউ
router.post("/update", authGurd("admin", "manager"), updateController);
```

## 🛡 TypeScript Fix (`express.d.ts`)

```ts
declare global {
  namespace Express {
    interface Request {
      user?: {
        id: string;
        email: string;
        name: string;
        role: string;
        emailVerified: boolean;
      };
    }
  }
}
```
