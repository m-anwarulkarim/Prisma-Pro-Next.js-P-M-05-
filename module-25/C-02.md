## 🔍 Prisma Dynamic Specific Words Search: বিস্তারিত আলোচনা

যখন আমরা চাই ইউজার ইনপুট বক্সে কিছু লিখলে সেই অনুযায়ী ডাটাবেস থেকে পোস্টের টাইটেল ফিল্টার হবে, তখন আমরা এই লজিকটি ব্যবহার করি। এটি মূলত ইউজারের দেওয়া কীওয়ার্ডের উপর ভিত্তি করে ডাইনামিক ডাটা খুঁজে বের করতে সাহায্য করে।

### 🖥️ কোড ব্রেকডাউন (Step-by-Step)

```ts
import { PrismaClient } from "@prisma/client";
import { Request, Response } from "express";

const prisma = new PrismaClient();

export const searchPostsByTitle = async (req: Request, res: Response) => {
  try {
    // ১. কুয়েরি থেকে সার্চ টেক্সট রিসিভ করা
    // URL: /posts?search=react হলে searchText এর মান হবে "react"
    // trim() ব্যবহার করা হয়েছে যাতে অনাকাঙ্ক্ষিত স্পেস বাদ যায়
    const searchText = String(req.query.search || "").trim();

    // ২. Prisma-এর মাধ্যমে ডাটাবেসে খোঁজা
    const posts = await prisma.post.findMany({
      where: {
        title: {
          contains: searchText, // টাইটেলের যেকোনো জায়গায় লেখাটি থাকলে মিলবে
          mode: "insensitive", // Case-insensitive (React = react = REACT)
        },
      },
      orderBy: {
        createdAt: "desc", // লেটেস্ট পোস্টগুলো সবার উপরে থাকবে
      },
    });

    // ৩. সাকসেস রেসপন্স পাঠানো
    return res.status(200).json({
      success: true,
      message: `Search results for "${searchText}"`,
      total: posts.length,
      data: posts,
    });
  } catch (error) {
    return res
      .status(500)
      .json({ success: false, message: "Error occurred", error });
  }
};
```

---

### 🧠 মূল কনসেপ্টসমূহ (Key Concepts)

#### ১. `contains` অপারেটর (SQL LIKE Operator)

Prisma-র `contains` অপারেটরটি অনেকটা SQL-এর `LIKE %text%` এর মতো কাজ করে। এটি নির্দিষ্ট ফিল্ডের শুরুতে, শেষে বা মাঝখানে যেখানেই শব্দটি থাকুক না কেন, তা খুঁজে বের করে।

- যদি টাইটেল হয়: **"Mastering Node.js and Express"**
- আর আপনার সার্চ টেক্সট হয়: **"Node"**
- যেহেতু মূল টাইটেলের ভেতরে "Node" শব্দটি আছে, তাই এটি রেজাল্ট হিসেবে চলে আসবে।

#### ২. `mode: "insensitive"` কেন ব্যবহার করবেন?

ডিফল্টভাবে PostgreSQL বা অন্য অনেক ডাটাবেস Case-sensitive সার্চ করে। অর্থাৎ যদি "node" লিখে সার্চ করেন আর ডাটাবেসে যদি "Node" থাকে, তবে সেটি খুঁজে পাবে না। `mode: "insensitive"` ব্যবহার করলে হাতের লেখা ছোট বা বড় যাই হোক না কেন, সঠিক ডাটা খুঁজে পাওয়া যায়।

#### ৩. Handling Empty Search (`""`)

যদি ইউজার কোনো সার্চ টেক্সট না পাঠায়, তবে `searchText` এর মান হবে খালি স্ট্রিং (`""`)। Prisma-তে `contains: ""` দিলে এটি টেবিলের সব ডাটা রিটার্ন করে। এটি একটি ভালো দিক, কারণ সার্চ বক্স খালি থাকা অবস্থায় ইউজার সাধারণত সব পোস্টই দেখতে চায়।

---

### 📊 ডাটাবেস কুয়েরি কিভাবে কাজ করে?

ইউজারের রিকোয়েস্ট থেকে ডাটাবেস পর্যন্ত প্রক্রিয়াটি নিচের টেবিলে দেখুন:

| Scenario           | URL Example      | Prisma Filter        | SQL Equivalent                        |
| ------------------ | ---------------- | -------------------- | ------------------------------------- |
| **Exact Search**   | `?search=Prisma` | `contains: "Prisma"` | `WHERE title LIKE '%Prisma%'`         |
| **Partial Search** | `?search=pri`    | `contains: "pri"`    | `WHERE title LIKE '%pri%'`            |
| **No Search**      | `/posts`         | `contains: ""`       | `WHERE title LIKE '%%'` (All records) |

---

### ✅ কেন এই পদ্ধতিটি সেরা?

- **User Experience (UX):** ইউজারকে একদম হুবহু বানান বা বড় হাতের/ছোট হাতের অক্ষর নিয়ে চিন্তা করতে হয় না।
- **Performance:** `findMany` এর সাথে `orderBy` ব্যবহার করলে ইউজার সবসময় লেটেস্ট আপডেটগুলো আগে দেখতে পায়।
- **Clean Code:** খুব অল্প কোডে এবং কোনো থার্ড-পার্টি লাইব্রেরি ছাড়াই শক্তিশালী সার্চ ইঞ্জিন তৈরি করা যায়।
