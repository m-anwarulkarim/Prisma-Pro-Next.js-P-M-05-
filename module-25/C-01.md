## 📊 Prisma `findMany()` (Fetch All) এর বিস্তারিত আলোচনা

এই ফাংশনটি মূলত একটি **"SELECT \* FROM Table"** কুয়েরির মতো কাজ করে। এটি ডাটাবেসের `post` টেবিলের প্রতিটি রেকর্ডকে একেকটি অবজেক্ট হিসেবে নিয়ে এসে একটি অ্যারে (Array) তৈরি করে।

### 🖥️ কোড বিশ্লেষণ

```ts
export async function getAllPosts() {
  // prisma.post.findMany() ডাটাবেসে যায় এবং সব পোস্ট সংগ্রহ করে
  const posts = await prisma.post.findMany();

  // প্রাপ্ত ডাটা (Array of Objects) রিটার্ন করে
  return posts;
}
```

---

### 💡 এই পদ্ধতির মূল বৈশিষ্ট্যসমূহ

1. **Array Return:** এটি সবসময় একটি অ্যারে রিটার্ন করবে। যদি টেবিলটি খালি থাকে, তবে এটি একটি খালি অ্যারে `[]` রিটার্ন করবে (null নয়)।
2. **No Filters:** যেহেতু এখানে `where` ক্লজ নেই, তাই এটি ডাটাবেসের প্রথম থেকে শেষ পর্যন্ত সব পোস্ট নিয়ে আসবে।
3. **Default Selection:** এটি প্রতিটি পোস্টের **সবগুলো ফিল্ড** (id, title, content, createdAt ইত্যাদি) নিয়ে আসে।

---

### ⚠️ কখন এটি ব্যবহার করতে হবে এবং কখন করতে হবে না?

**✅ ব্যবহার করতে হবে যখন:**

- আপনার টেবিলের ডাটা খুব কম (যেমন: ১০-৫০টি ক্যাটাগরি বা ড্রপডাউন লিস্ট)।
- অ্যাডমিন প্যানেলে সব ইউজারের লিস্ট দেখার প্রয়োজন হলে।

**❌ ব্যবহার করতে হবে না যখন:**

- টেবিলে হাজার হাজার বা লক্ষ লক্ষ ডাটা আছে। এতে সার্ভারের মেমোরিতে চাপ পড়বে এবং অ্যাপ স্লো হয়ে যাবে।
- বড় টেবিলের ক্ষেত্রে সবসময় **Pagination** (যেমন: `take` এবং `skip`) ব্যবহার করা উচিত।

---

### 🛠️ ডাটাবেস থেকে রেসপন্সটি দেখতে কেমন হবে?

যখন আপনি `getAllPosts()` কল করবেন, তখন সেটি নিচের মতো একটি JSON ডাটা রিটার্ন করবে:

```json
[
  {
    "id": 1,
    "title": "First Blog Post",
    "content": "This is my first post content.",
    "createdAt": "2024-03-20T10:00:00.000Z"
  },
  {
    "id": 2,
    "title": "Learning Prisma",
    "content": "Prisma makes database handling easy!",
    "createdAt": "2024-03-21T12:30:00.000Z"
  }
]
```

---

### 🚀 টিপস: `select` ব্যবহার করা

সব সময় সব ডাটা না নিয়ে শুধু প্রয়োজনীয় ফিল্ডগুলো নেওয়া ভালো। এতে ডাটাবেসের ওপর চাপ কমে।

```ts
// উদাহরণ: শুধু টাইটেল আর আইডি নেওয়ার জন্য
const posts = await prisma.post.findMany({
  select: {
    id: true,
    title: true,
  },
});
```
